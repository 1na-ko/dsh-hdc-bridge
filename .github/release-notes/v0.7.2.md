# v0.7.2 — README 配图（面板截图）

按 dsh-market 截图机制的要求为 README 增加插件面板截图（市场/ npm / GitHub 三处自动展示）。代码无功能变更，仅文档与打包清单。

## 变更 / Changes

- README 新增「截图 / Screenshots」章节：浮动设备面板特写（官方主题、设备列表、系统区、工具链徽章、hilog 尾部）
- 截图存放于 `docs/screenshots/panel.png`（无头浏览器实机抓取，仅插件自身 UI，无会话内容）
- `package.json` `files` 增加 `docs`，确保图片随包发布
